const order = require('./order');
const quote = require('./quote');
const operations = require('./operations');
const billData = require('./billData');

module.exports = {
	order,
	quote,
	operations,
	billData
}